# Manifest Samples

`manifest/` contains example endpoint manifests that can be seeded into a project.

Endpoint manifests help the detector normalize known application routes during warmup. This reduces false novelty when a project has many parameterized URLs.

`example_manifest.json` is a sanitized sample. Replace service names, paths, and metadata with the routes from your own application before using it.
