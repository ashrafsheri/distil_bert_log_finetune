apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: anomaly-state-pvc
  namespace: logguard
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 2Gi
